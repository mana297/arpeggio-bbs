# ARPEGGIO バグログ

バグ修正・エンバグの記録。バグを修正したタイミングで必ずここに追記する。

---

## 記録フォーマット

```
### BUG-XXX: バグ概要
- **確認バージョン**: vX.Y
- **バグ内容**: 症状・原因・該当コード箇所
- **修正日**: YYYY-MM-DD
- **修正内容**: どう直したか
- **種別**: 通常バグ / エンバグ / 軽微
```

---

## 修正済みバグ

### BUG-001: ボード関連バグ・入室通知・日付セパレーターの不具合
- **確認バージョン**: v0.3相当
- **バグ内容**: 掲示板の表示不具合・入退室通知の誤発火・日付セパレーターの重複表示
- **修正日**: 2026-03-xx（git: `fix: board bug, notices, date sep, name save`）
- **修正内容**: 各バグを個別修正。名前の localStorage 保存ロジックも修正
- **種別**: 通常バグ

### BUG-002: iOS でスラッシュコマンドが認識されない
- **確認バージョン**: v0.5相当
- **バグ内容**: iOS の日本語キーボードで全角スラッシュ（／）が入力されるため `/help` 等が認識されない
- **修正日**: 2026-03-xx（git: `fix: iOS slash commands, presence reconnect`）
- **修正内容**: `normalizeSlash()` 関数で全角スラッシュを半角に正規化
- **種別**: 通常バグ

### BUG-003: ページリロード後プレゼンスが再接続されない
- **確認バージョン**: v0.5相当
- **バグ内容**: Firebase Realtime DB の接続が切断後に復帰しないケースがあった
- **修正日**: 2026-03-xx（git: `fix: iOS slash commands, presence reconnect`）
- **修正内容**: `.info/connected` の `onValue` でプレゼンス再設定ロジックを追加
- **種別**: 通常バグ

---

## 既知・未修正バグ（修正予定あり）

（なし）
