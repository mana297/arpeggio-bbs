# 開発体制・フロー 引き継ぎドキュメント

> このドキュメントは「大賢者プロジェクト（daigenja）」で運用してきた開発体制を  
> 他プロジェクトに引き継ぐためのテキストベースのまとめです。  
> プロジェクト固有の部分は適宜読み替えて使用してください。

---

## 1. 開発ドキュメント体制

どのプロジェクトでも以下の4ファイルを必ず置く。

| ファイル | 役割 | 更新タイミング |
|---------|------|--------------|
| `BUGLOG.md` | バグ・エンバグの詳細記録 | バグ発見・修正のたびに |
| `FEATURELOG.md` | 全機能の実装記録（通し番号）| 新機能・アーキテクチャ改善のたびに |
| `CHANGELOG.md` | バージョン別の変更概要 | バージョン完了時 |
| `README.md` | セットアップ・概要（GitHub向け）| 大きな仕様変更時 |

> **ポイント**: BUGLOG と FEATURELOG は「後で見返す」ためのもの。  
> 書くのが面倒でも「このバグは何が原因だったか」「この機能はどこに実装されているか」  
> が1ファイルで分かる状態を維持する。

---

## 2. バージョン管理ポリシー

### 基本ルール
- **メジャー・マイナー・パッチは同一セッションで混ぜない**
- **バージョンは必ず1つずつ進める**（v1.0 → v1.1 → v2.0 など）
- 各バージョン完了後はユーザーと一言確認してから次へ

### サブバージョン制（推奨）
大きなマイルストーンに向けて小刻みに進める方式。

```
v1.0  ← マイルストーン（大きなまとまり）
  v1.0.1  ← 1セッション = 1〜3機能
  v1.0.2  ← 1セッション = 1〜3機能
  v1.0.3  ← ホットフィックスも1サブバージョン使う
  ...
v1.1  ← 次のマイルストーン完成
```

**ホットフィックスもサブバージョン1つ消費する。**  
緊急バグ修正が入ったら予定していた機能追加を1つ後ろにずらす。

### バージョン番号の命名感覚
| 種別 | タイミング |
|------|-----------|
| パッチ `x.y.z` | バグ修正・ホットフィックス・小改善 |
| マイナー `x.y` | 機能群のまとまり・設計改善 |
| メジャー `x` | コンセプトの転換・大規模リアーキテクチャ |

---

## 3. バグログ（BUGLOG.md）の書き方

### フォーマット

```markdown
### BUG-XXX: バグの一言概要
- **確認バージョン**: vX.Y（どのバージョンで発覚したか）
- **バグ内容**: 症状・原因・該当コード箇所（できるだけ具体的に）
- **修正日**: YYYY-MM-DD
- **修正内容**: どう直したか
- **種別**: 通常バグ / エンバグ / 軽微
```

### 種別の使い分け
| 種別 | 説明 |
|------|------|
| `通常バグ` | 実装ミスや見落としによるバグ |
| `エンバグ` | 別のバグを直したことで生まれた新しいバグ |
| `軽微` | 動作に支障はないがコードが汚い・非効率な箇所 |

### 未修正バグ（PENDING）の扱い
修正方法は分かっているが今すぐ直さないバグは別セクションに置く。

```markdown
## 既知・未修正バグ（修正予定あり）

### PENDING-XXX: バグ概要
- **確認バージョン**: ...
- **内容**: ...
- **修正方法**: ...（分かっていれば）
- **対応方針**: 修正待ち（次回セッションで対応） / 放置（理由: ...）
```

**「放置」は正式な選択肢。** 放置と気づいていないを区別することが重要。  
放置する場合は必ず「放置（理由: ...）」と理由を書く。

### バグ重要度分類
| レベル | 基準 | 対応方針 |
|--------|------|---------|
| P0（致命）| クラッシュ・起動不可・データロスト | 即修正（バージョン関係なく） |
| P1（重要）| 機能が使えない・ユーザーに見える不具合 | バージョン境界で修正 |
| P2（軽微）| パフォーマンス劣化・ログが汚い | 判断して「放置」もあり |

---

## 4. 実装ログ（FEATURELOG.md）の書き方

### フォーマット

```markdown
### FEAT-XXX: 機能の一言概要
- **実装バージョン**: vX.Y
- **実装日**: YYYY-MM-DD
- **種別**: 新機能 / アーキテクチャ / UI / バグ修正
- **実装内容**: 何を・どこに・どう実装したか（具体的に）
- **関連コード**: 関数名・クラス名・ファイル名
```

### 通し番号の管理
- `FEAT-001` から始めて通し番号で管理
- バグ修正も実装ログに記録する（種別: `バグ修正`）
- 番号は欠番を作らず連番で振る

---

## 5. CHANGELOG.md の書き方

```markdown
## [vX.Y.Z] YYYY-MM-DD — 一言概要

### Added（新機能）
- 追加した機能の概要

### Changed（変更）
- 変更した挙動の概要

### Fixed（バグ修正）
- BUG-XXX: 修正したバグの概要（詳細はBUGLOG参照）

### Architecture（設計改善）
- アーキテクチャ・コード品質改善の概要
```

> CHANGELOG は「概要」、BUGLOG / FEATURELOG は「詳細」という役割分担。  
> CHANGELOGに全部書こうとしない。

---

## 6. Gitコミット規約

### コミットメッセージのフォーマット
```
<type>: <一行で何をしたか>

- 変更点1の詳細（必要なら）
- 変更点2の詳細（必要なら）

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
```

### typeの使い分け
| type | 使う場面 |
|------|---------|
| `feat` | 新機能追加 |
| `fix` | バグ修正 |
| `docs` | ドキュメント（BUGLOG/CHANGELOG/README等）の更新 |
| `refactor` | コードの整理・リファクタリング |
| `chore` | 設定ファイル・.gitignore等の変更 |

### タグの付け方
バージョン完了時はコミットにタグを打つ。

```bash
git tag vX.Y.Z
git push origin main --tags
```

### Claude Code が自動コミットするタイミング
| タイミング | 操作 |
|-----------|------|
| バージョン完了時 | commit + tag + push |
| バグ修正完了時（BUGLOG記録済み）| commit + push |
| ドキュメント更新時 | commit + push |
| セッション終了前に未コミットがある時 | まとめてcommit + push |

コミット前に必ず**構文チェック**を実施する。

```bash
python -m py_compile <メインファイル>.py
```

---

## 7. 実装後の定常タスクフロー（毎回必ず実施）

実装・変更を加えたら、コードを書き終えた後に**以下を順番に実施**する。

### Step 1: バグチェック
実装・変更を加えたら必ず確認する:
- 同じデータ構造を扱う他の関数に同パターンのバグがないか
- エラーハンドリングの範囲が適切か（広すぎる `except Exception` で握りつぶしていないか）
- マルチスレッド・非同期処理でのデータ競合リスクがないか

### Step 2: エンバグチェック（修正後に必ず実施）
バグを直したら「その修正が新たなバグを生んでいないか」を確認する:

- 新たに参照している属性・変数がそのスコープに実際に存在するか
- 発症が遅延するバグ（タイマー系・ループ系・デーモン系）を混入させていないか
- 早期 return / 例外パスでもリソース解放・状態リセットが行われるか

**典型的なエンバグパターン（過去の実例より）:**
- 「別クラスの属性を誤参照」（`AttributeError` が後から出る）
- 「配列長のズレ」（2つの配列をペアで扱っているのに一方しか更新しない）
- 「スレッドステータス放置」（早期return時に状態リセットを忘れる）
- 「import忘れ」（ローカルでは手動importしてたため気づかない — 特に `requests` 等）
- 「例外握りつぶし」（`except Exception` で `None` を返してサイレント失敗）

### Step 3: バグログ更新（BUGLOG.md）
発見・修正したバグを記録する。

### Step 3.5: 実装ログ更新（FEATURELOG.md）
新機能・アーキテクチャ改善を記録する（FEAT-XXX 採番）。

### Step 4: 構文チェック + Gitコミット
```bash
python -m py_compile <ファイル>.py
git add .
git commit -m "fix/feat/docs: ..."
git push
```

### Step 5: CHANGELOG.md 更新（バージョン完了時のみ）

---

## 8. 定期メンテナンス

**タイミング:** 各バージョン完了後・次バージョン着手前に1回

チェック内容:
- 実装したコードに新しいバグが生まれていないか
- PENDING バグのうち対応できるものが出てきていないか
- ドキュメント（BUGLOG / FEATURELOG / CHANGELOG）が現実と一致しているか
- Gitが正しくpushされているか（リモートと同期確認）

---

## 9. セキュリティ制約（必ず守る）

- **APIキー・認証情報は絶対に出力・コミットしない**
  - `.gitignore` に `config.json` / `.env` / `secrets.*` を入れること
  - コード内にハードコードしない（環境変数 or 設定ファイル経由）
- **ユーザーのファイルは許可なく削除しない**
  - 削除前に必ず確認するか、削除対象が明確な場合のみ実施
- **外部API通信は `try/except` で囲み、失敗時は明示的にエラーを返す**
  - `except Exception` で握りつぶして `None` を返すのは厳禁

---

## 10. Claude Code との作業ルール

### セッション管理
- セッションをまたぐ場合、Claude Code はメモリ（`~/.claude/projects/.../memory/`）に状態を保存する
- 会話が圧縮・要約されても作業が続けられるよう、重要な決定はドキュメントファイルに書く

### 指示の出し方
- 「バージョン1つずつ」「マイナー・メジャーを混ぜない」などのルールは毎回再指示不要（メモリに記録済み）
- 差し込み依頼は随時OK。ただし現在の作業との優先度を一言確認してから進める
- 「方針をクロードに委ねる」でも良い。ただし理由と選択肢は必ず提示して決定する

### コミットのタイミング
- ユーザーが「コミットして」と言わなくても、バージョン完了・バグ修正完了時は自動でコミット＆プッシュ
- 「コミットのタイミングが良さそうなら自分で判断してやって」という前提で動く

---

## 11. ファイル構成テンプレート

新プロジェクトを始める際の最小構成:

```
project/
├── main.py（またはindex.html等）
├── config.json          ← .gitignoreに入れる（APIキー等）
├── requirements.txt     ← pip freeze > requirements.txt
├── README.md
├── CHANGELOG.md
├── BUGLOG.md
├── FEATURELOG.md
├── .gitignore
└── backup/              ← バックアップ自動生成先（.gitignoreに入れる）
```

### .gitignore 最低限
```
config.json
*.db
backup/
*.log
__pycache__/
*.pyc
.env
*.migrated
.toast_clicked
```

---

## 12. BUGLOG / FEATURELOG の初期テンプレート

### BUGLOG.md 初期テンプレート
```markdown
# [プロジェクト名] バグログ

バグ修正・エンバグの記録。バグを修正したタイミングで必ずここに追記する。

---

## 記録フォーマット

### BUG-XXX: バグ概要
- **確認バージョン**: vX.Y
- **バグ内容**: 症状・原因・該当コード箇所
- **修正日**: YYYY-MM-DD
- **修正内容**: どう直したか
- **種別**: 通常バグ / エンバグ / 軽微

---

## 既知・未修正バグ（修正予定あり）

（なし）
```

### FEATURELOG.md 初期テンプレート
```markdown
# [プロジェクト名] 実装ログ

全機能の実装記録。新機能・アーキテクチャ改善を追加したタイミングで必ずここに追記する。

---

## 記録フォーマット

### FEAT-XXX: 機能概要
- **実装バージョン**: vX.Y
- **実装日**: YYYY-MM-DD
- **種別**: 新機能 / アーキテクチャ / UI / バグ修正
- **実装内容**: 何を・どこに・どう実装したか
- **関連コード**: 関数名・クラス名・ファイル名

---

## v1.0 — 初期実装

### FEAT-001: ...
```

### CHANGELOG.md 初期テンプレート
```markdown
# CHANGELOG

[プロジェクト名]の変更履歴。バージョン完了時に必ず更新する。
バグの詳細は [BUGLOG.md](BUGLOG.md) を参照。

フォーマット: Added（新機能）/ Changed（変更）/ Fixed（バグ修正）/ Architecture（設計改善）

---

## [v1.0.0] YYYY-MM-DD — 初期リリース

### Added
- 初期機能
```

---

## 13. コードの分散化・モジュール化（1ファイル肥大化の防止）

> **教訓（大賢者プロジェクトより）**: 1ファイルにすべてを詰め込むと、機能が増えるほど「どこに何があるか」が分からなくなり、修正のたびにファイル全体を把握する必要が生じる。  
> C++ が `.h`（宣言）と `.cpp`（実装）を責務単位で分けるように、**機能ごとにファイルを分ける**ことで肥大化を防ぐ。

### 基本原則

- **1ファイル = 1つの責務**（チャット・掲示板・ステータス・認証 は別ファイル）
- **目安: 200〜300行を超えたら分割を検討する**
- 新機能追加時は、既存ファイルに追記するのではなく **新しいファイルを作ることを先に検討**する
- 分割後も動作するよう、**依存関係は明示的に** `import` / `<script src>` する

### 分割の単位（責務で分ける）

| 分割単位 | 例 |
|---------|-----|
| 機能ドメイン | `chat.js` / `board.js` / `status.js` / `dm.js` |
| レイヤー | `ui.js`（表示） / `firebase.js`（DB通信） / `utils.js`（共通関数）|
| 設定・定数 | `config.js` / `constants.js` |
| エントリーポイント | `main.js`（全体の起動・初期化のみ、100行以内を目標） |

### Web プロジェクトの推奨構成

```
project/
├── index.html              ← HTML構造のみ（scriptやstyleは外部ファイル参照）
├── css/
│   ├── base.css            ← リセット・変数・共通スタイル
│   ├── layout.css          ← ヘッダー・タブ・パネル等の骨格
│   ├── chat.css            ← チャット固有スタイル
│   ├── board.css           ← 掲示板固有スタイル
│   └── components.css      ← ボタン・モーダル・バッジ等の部品
├── js/
│   ├── main.js             ← エントリーポイント（initのみ）
│   ├── firebase.js         ← Firebase初期化・接続
│   ├── auth.js             ← 認証・パスワード入室
│   ├── chat.js             ← チャット送受信・コマンド
│   ├── board.js            ← 掲示板・いいね・リプライ
│   ├── status.js           ← ステータスタグ
│   ├── members.js          ← メンバーリスト・プレゼンス
│   └── utils.js            ← 共通関数（avClass, fmt 等）
├── README.md
├── CHANGELOG.md
├── BUGLOG.md
└── FEATURELOG.md
```

### Python プロジェクトの推奨構成

```
project/
├── main.py                 ← エントリーポイント（起動のみ、100行以内を目標）
├── config.py               ← 設定・定数
├── ui.py                   ← UI構築・表示ロジック
├── [機能名].py              ← 機能ごとに1ファイル（chat.py, status.py 等）
├── utils.py                ← 共通ユーティリティ
└── (ドキュメント類)
```

### 分割のタイミング

| タイミング | 対応 |
|-----------|------|
| 新機能追加前にファイルが肥大化している | **先に分割してからバージョンを消費**し、次のバージョンで機能追加 |
| 新機能が既存コードと明らかに責務が異なる | 最初から新ファイルに書く |
| 1ファイルが 300 行を超えた | 分割を検討（次バージョンで対応可） |
| 「どこに書いたか分からない」が起き始めた | 遅くとも今すぐ分割 |

> **分割はバージョンを1つ消費する。**  
> `refactor: チャット機能を js/chat.js に分離` のように独立したバージョンとして切る。  
> 分割と機能追加を同時にやると混乱しやすいため、**分割してから追加**の順番を守る。

### やってはいけないこと

- 「とりあえず動いてるから分割しない」→ 技術的負債が積み重なる
- 「全部 main.js / main.py に書く」→ 1000行超えで手に負えなくなる（大賢者の教訓）
- 分割と機能追加を同じバージョンでやる → 問題が起きたとき原因の切り分けができない

---

*このドキュメントは大賢者プロジェクト（v3.5.3時点）の開発体制をベースに作成。*  
*2026-05-24 作成 / セクション13（コード分散化）追加: 2026-05-24*
